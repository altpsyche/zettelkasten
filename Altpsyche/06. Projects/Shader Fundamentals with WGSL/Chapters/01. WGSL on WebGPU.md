---
type: project
chapter: 1
idea:
artwork:
created: 2026-09-01
status: drafting
tags:
  - project
  - wgsl
---

# 01. WGSL on WebGPU

Part of [[Series Outline]].

## The one new idea


## Artwork


## Reader can do at the end


## Prerequisites

None. Every example runs in the altpsyche.dev playground, so there is nothing to install and no renderer to build.


## Beats

**Intro: why WebGPU exists**
1. One portable API. OpenGL, 1992, ran everywhere.
2. It stopped fitting the hardware. Vulkan, Metal and DirectX 12 each replaced it on one platform.
3. That bought speed and cost portability. Four modern APIs, none of them everywhere.
4. WebGPU is the portable layer back on top of Vulkan, Metal and DirectX.
5. So it is not a new low level API. It wraps the three that already exist.
6. What WebGL never had: compute shaders, indirect drawing, render bundles, external textures, flexible canvas integration, better debugging.
7. And no global state, which was WebGL's main design flaw.
8. Of that list this series uses compute. The rest is renderer work.
9. Not browser only. Browsers through JavaScript, TypeScript and WASM. Node. Native through Dawn for C and C++, wgpu for Rust. Community bindings past that.

**For GLSL readers**
10. What carries over unchanged, and what does not.
11. The remap they need: types, entry points, bindings, no global state.

**What WGSL is**
12. One language, three stages: vertex, fragment, compute.
13. This series uses fragment and compute. No vertex stage, no textures.
14. Where the code will live. The playground, nothing to install.

**Close**
15. What chapter 2 starts.

## Working example


## Where readers will get stuck
- 

## Cut
- 

## Built on
- [[]] — 

## References
- [[WebGPU Fundamentals]] — 
- [[What is WebGPU]] — fragmentation story and the API timeline behind beats 1 to 5. his triangle line counts are unverified, check before quoting a number.
- [[WebGPU WGSL]] — language reference for beats 10 and 11. types, entry points, builtin attributes.
- [[Tour of WGSL]] — Google's interactive tour. closest model for how the playground chapters should read.
- [[Introduction to WebGPU (CIS 565)]] — beats 6 to 9. Jones edits the spec, so the feature list and the platform list come from here.
- [[WGSL Specification]] — normative. settle anything the other sources disagree on before publishing.

## Draft

Optional colour for beats 1 to 3, from [[What is WebGPU]]: the console era (PS1, PS2, N64, GameCube, Wii, PS3, Xbox 360) shows OpenGL and DirectX ran the industry from roughly 1990 to 2010. Fun, not load bearing. Cut unless it earns space.

### Intro, beats 1 to 9

The GPU takes no instructions from you directly. A graphics API sits in between, and the one you write for shapes what your shader code looks like. WGSL exists because of a particular API, and that API exists because of a particular problem.

In 1992 Silicon Graphics released OpenGL. One API, one set of calls, running on whatever hardware you had. That held for about two decades. DirectX arrived in 2001 and split the world in two, and two was manageable.

Then the hardware moved. GPUs stopped resembling the machines OpenGL had been designed to talk to, and the abstraction started costing real performance. The answer came from three directions at once, and it was to throw the abstraction out. Apple built Metal. Khronos built Vulkan. Microsoft built DirectX 12. Each is faster than what it replaced. Each runs on exactly one platform family.

That trade is the whole story. Speed went up. Portability went to zero.

WebGPU is the portability put back. One API, sitting on top of Vulkan, Metal and DirectX, translating your calls into whichever of the three is underneath.

The name misleads on two counts, so both are worth stating plainly.

First, WebGPU is not a new low level API competing with Vulkan. It is a layer over the three that already exist. That is how it manages to be portable and fast at the same time.

Second, WebGPU is not confined to browsers. It runs in browsers through JavaScript, TypeScript and WebAssembly. It runs in Node. It runs natively through Dawn for C and C++, and through wgpu for Rust, with community bindings past those. Bevy, the Rust game engine, is built on wgpu.

Against WebGL, the browser API it replaces, WebGPU adds compute shaders, indirect drawing, render bundles, external textures, better canvas integration and better debugging. Most of that list belongs to renderer work. Compute is the one this series comes back to.

It also drops global state. In WebGL you bind a buffer, bind a texture, select a program, and the draw call uses whatever happened to be bound. Nothing in the draw call says what it is drawing with, so a function that works alone breaks when something else ran first and left the wrong thing bound. WebGPU makes you declare the inputs up front instead.
